# Local-only secrets for the dev proxy (server/proxy.mjs).
# Copy this file to server/.env and fill in the value, then run: npm run proxy
# server/.env is gitignored and must never be committed or included in a build.
ANTHROPIC_API_KEY=
